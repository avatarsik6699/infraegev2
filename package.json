{
  "name": "infraege",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0",
    "pnpm": "10.33.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "lint:fix": "pnpm -r --if-present lint:fix",
    "format": "prettier . --write && cd apps/api && uv run ruff format app tests migrations ../../ops ../../scripts/application_db.py ../../scripts/lib/application_db ../../scripts/tests/application_db_test.py ../../scripts/tests/deploy_orchestration_test.py",
    "format:check": "prettier . --check && cd apps/api && uv run ruff format --check app tests migrations ../../ops ../../scripts/application_db.py ../../scripts/lib/application_db ../../scripts/tests/application_db_test.py ../../scripts/tests/deploy_orchestration_test.py",
    "api:generate": "node scripts/api-contracts.mjs",
    "api:check": "node scripts/api-contracts.mjs --check",
    "images:generate": "node scripts/generate-responsive-images.mjs",
    "brand:generate": "node scripts/generate-brand-assets.mjs",
    "test:content-assets": "node --test scripts/tests/task-content-assets.test.mjs",
    "validate:content": "node scripts/validate-content-links.mjs",
    "audit:a11y": "pnpm --filter web exec playwright test e2e/accessibility.spec.ts",
    "audit:performance": "bash scripts/run-lighthouse-audit.sh lhci",
    "audit:security": "bash scripts/security-gate.sh",
    "audit:images": "bash scripts/image-gate.sh",
    "lint:shell": "bash scripts/check-shell.sh"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@lhci/cli": "0.15.1",
    "prettier": "3.9.6",
    "pyright": "1.1.411"
  },
  "version": "1.0.0",
  "description": "Веб-приложение для подготовки к ЕГЭ по информатике с опубликованными уроками, связанной практикой и локальным прогрессом.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avatarsik6699/infraegev2.git"
  },
  "bugs": {
    "url": "https://github.com/avatarsik6699/infraegev2/issues"
  },
  "homepage": "https://github.com/avatarsik6699/infraegev2#readme"
}
