# Редакционная поставка Practice UX

Пакет `practice-ux-8a59a7d4a30a0440bdff3265`, SHA-256
`4fdca8fc19735af68bc9664fc3c5b0f9072232b7edaafc8b024e50cb49c4bada`.
Применяется после обоих пакетов Change 120 на схеме `121_01` с `--update`.
Задачи ожидают revision 1 либо 2 согласно предыдущей редакционной поставке;
конфликт с более свежими операторскими правками требует нового экспорта, а не перезаписи.

547 задач получили краткое описание и навык; 516 методических пояснений отмечены как
`method`, 31 исходный разбор оставлен `unclassified`: происхождение текста не доказывает
полноту решения. Полные решения не выдумывались, исходные ответы не пересчитывались.
Сохранены UUID, исходные ответы, публичные/приватные источники, связи и solution_revision=1.

Восемь явно перечисленных исходных таблиц №16 преобразованы в группы вариантов кода.
Каждый листинг и язык сохранены; код не исполнялся и исходные неточности не исправлялись.
Математические фрагменты исходного условия получили явный тип formula; проверка обратного
преобразования всех 547 условий подтверждает сохранность символов и исходной структуры.
Исходный снимок не содержит буквальных backtick: они были добавлены конвертером 120 только
при обработке формул LaTeX. Авторские подсказки/пояснения с inline-кодом не переклассифицируются.

Пакет воспроизводится офлайн в **новую** директорию:

```bash
cd apps/api
uv run python -m app.modules.practice.ux_editorial \
  --source ../../content/practice-imports/120-ege-5-16 \
  --output /absolute/new-directory/package \
  --registry practice-registry.json
```

`report.json` фиксирует распределение навыков, классификацию и allowlist групп кода.
Подготовленный пакет неизменяем; дальнейшие исправления — новым пакетом с актуальными revisions.
Это операторские данные, их нельзя копировать в public или frontend bundle.

На dev пакет применён 2026-09-16 штатным CLI с pre/post backups. Восстановление post-backup
`5afa487b` в изолированную PostgreSQL прошло проверку всех 697 задач, включая checker и
новые блоки/поля. Production не изменялся. Неизвестное время остаётся null; демонстрационная
статистика в этих данных отсутствует.

Повторное применение вернуло `already_committed`; операторский экспорт подтвердил сохранение
новых полей и группы кода с текущей revision. Пакет не изменялся после применения.
