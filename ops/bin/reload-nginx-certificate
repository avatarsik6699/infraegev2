#!/usr/bin/env bash
set -euo pipefail

container_id=$(docker ps --quiet --filter label=com.infraege.service=nginx)
if [[ -z $container_id ]]; then
  echo "infraege Nginx container is not running" >&2
  exit 1
fi

docker kill --signal HUP "$container_id" >/dev/null
