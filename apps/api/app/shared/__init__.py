"""Cross-module code shared by >= 2 modules.

Stays empty until a second module actually needs something a first module already has —
promote it here then, rather than pre-populating with speculative helpers.
"""
