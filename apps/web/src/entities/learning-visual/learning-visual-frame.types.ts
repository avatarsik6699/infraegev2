export namespace LearningVisualFrameTypes {
  export type Props = {
    children: React.ReactNode;
    caption: string;
    purpose: string;
    accessibleDescription: string;
    className?: string;
  };
}
