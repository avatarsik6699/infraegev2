export namespace SearchFieldTypes {
  export type Props = {
    label: string;
    name: string;
    placeholder: string;
    defaultValue?: string;
    maxLength?: number;
    submitLabel: string;
    clearLabel: string;
  };
}
